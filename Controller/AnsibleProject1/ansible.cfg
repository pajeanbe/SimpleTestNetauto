[defaults]
# Use the inventory directory for host files
inventory = inventory/


# Path to roles
roles_path = roles/

# Disable host key checking (for simplicity in dev environments, use with caution)
host_key_checking = False

# Default user to connect with
remote_user = ansible